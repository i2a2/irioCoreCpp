name: Documentation

on: 
    workflow_dispatch:

jobs:
    generate_and_publish:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v4
            - run: sudo apt install -y doxygen

            - run: make doc