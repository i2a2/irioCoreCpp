name: Compile and Test Stages

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]
    paths:
      - '**.cpp'
      - '**.h'
  pull_request:
    branches: [ "main" ]
    paths:
      - '**.cpp'
      - '**.h'
jobs:
  compile:
    runs-on: ubuntu-latest
    container: 
      image: ghcr.io/i2a2/iriocore-docker:v1.0
    steps:
    - uses: actions/checkout@v3  
    - name: Compile Code
      run: make compile -j

    - name: Run UnitTests
      run: make test
